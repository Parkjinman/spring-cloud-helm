apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "woodpecker-helm.fullname" . }}-batch-deployment
  labels:
    app: woodpecker-batch
  {{- include "woodpecker-helm.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.batchDeployment.replicas }}
  selector:
    matchLabels:
      app: woodpecker-batch
    {{- include "woodpecker-helm.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app: woodpecker-batch
      {{- include "woodpecker-helm.selectorLabels" . | nindent 8 }}
    spec:
      containers:
      - env:
        - name: spring.profiles.active
          value: prod
        image: {{ .Values.batchDeployment.batch.image.repository }}:{{ .Values.batchDeployment.batch.image.tag
          | default .Chart.AppVersion }}
        name: batch
        ports:
        - containerPort: 8020
        resources: {}
      nodeSelector:
        woodpecker-node: "true"
      restartPolicy: Always
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "woodpecker-helm.fullname" . }}-bff-deployment
  labels:
    app: woodpecker-bff
  {{- include "woodpecker-helm.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.bffDeployment.replicas }}
  selector:
    matchLabels:
      app: woodpecker-bff
    {{- include "woodpecker-helm.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app: woodpecker-bff
      {{- include "woodpecker-helm.selectorLabels" . | nindent 8 }}
    spec:
      containers:
      - env:
        - name: spring.profiles.active
          value: prod
        image: {{ .Values.bffDeployment.bff.image.repository }}:{{ .Values.bffDeployment.bff.image.tag
          | default .Chart.AppVersion }}
        name: bff
        ports:
        - containerPort: 8040
        resources: {}
      nodeSelector:
        woodpecker-node: "true"
      restartPolicy: Always
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "woodpecker-helm.fullname" . }}-discovery-deployment
  labels:
    app: woodpecker-discovery
  {{- include "woodpecker-helm.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.discoveryDeployment.replicas }}
  selector:
    matchLabels:
      app: woodpecker-discovery
    {{- include "woodpecker-helm.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app: woodpecker-discovery
      {{- include "woodpecker-helm.selectorLabels" . | nindent 8 }}
    spec:
      containers:
      - env:
        - name: spring.profiles.active
          value: prod
        - name: eureka.environment
          value: prod
        image: {{ .Values.discoveryDeployment.discovery.image.repository }}:{{ .Values.discoveryDeployment.discovery.image.tag
          | default .Chart.AppVersion }}
        name: discovery
        ports:
        - containerPort: 8761
        resources: {}
      nodeSelector:
        woodpecker-node: "true"
      restartPolicy: Always
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "woodpecker-helm.fullname" . }}-gateway-deployment
  labels:
    app: woodpecker-gateway
  {{- include "woodpecker-helm.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.gatewayDeployment.replicas }}
  selector:
    matchLabels:
      app: woodpecker-gateway
    {{- include "woodpecker-helm.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app: woodpecker-gateway
      {{- include "woodpecker-helm.selectorLabels" . | nindent 8 }}
    spec:
      containers:
      - env:
        - name: spring.profiles.active
          value: prod
        image: {{ .Values.gatewayDeployment.gateway.image.repository }}:{{ .Values.gatewayDeployment.gateway.image.tag
          | default .Chart.AppVersion }}
        name: gateway
        ports:
        - containerPort: 9000
        resources: {}
      nodeSelector:
        woodpecker-node: "true"
      restartPolicy: Always
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "woodpecker-helm.fullname" . }}-kubernetes-deployment
  labels:
    app: woodpecker-kubernetes
  {{- include "woodpecker-helm.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.kubernetesDeployment.replicas }}
  selector:
    matchLabels:
      app: woodpecker-kubernetes
    {{- include "woodpecker-helm.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app: woodpecker-kubernetes
      {{- include "woodpecker-helm.selectorLabels" . | nindent 8 }}
    spec:
      containers:
      - env:
        - name: spring.profiles.active
          value: prod
        image: {{ .Values.kubernetesDeployment.kubernetes.image.repository }}:{{ .Values.kubernetesDeployment.kubernetes.image.tag
          | default .Chart.AppVersion }}
        name: kubernetes
        ports:
        - containerPort: 8030
        resources: {}
      nodeSelector:
        woodpecker-node: "true"
      restartPolicy: Always
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "woodpecker-helm.fullname" . }}-postgres-deployment
  labels:
    app: woodpecker-postgres
  {{- include "woodpecker-helm.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.postgresDeployment.replicas }}
  selector:
    matchLabels:
      app: woodpecker-postgres
    {{- include "woodpecker-helm.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app: woodpecker-postgres
      {{- include "woodpecker-helm.selectorLabels" . | nindent 8 }}
    spec:
      containers:
      - image: {{ .Values.postgresDeployment.postgres.image.repository }}:{{ .Values.postgresDeployment.postgres.image.tag
          | default .Chart.AppVersion }}
        name: postgres
        ports:
        - containerPort: 5432
        resources: {}
      nodeSelector:
        woodpecker-node: "true"
      restartPolicy: Always
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "woodpecker-helm.fullname" . }}-user-deployment
  labels:
    app: woodpecker-user
  {{- include "woodpecker-helm.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.userDeployment.replicas }}
  selector:
    matchLabels:
      app: woodpecker-user
    {{- include "woodpecker-helm.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app: woodpecker-user
      {{- include "woodpecker-helm.selectorLabels" . | nindent 8 }}
    spec:
      containers:
      - env:
        - name: spring.profiles.active
          value: prod
        image: {{ .Values.userDeployment.user.image.repository }}:{{ .Values.userDeployment.user.image.tag
          | default .Chart.AppVersion }}
        name: user
        ports:
        - containerPort: 8010
        resources: {}
      nodeSelector:
        woodpecker-node: "true"
      restartPolicy: Always