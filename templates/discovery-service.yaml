apiVersion: v1
kind: Service
metadata:
  name: discovery-service
  labels:
  {{- include "spring-cloud-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.discoveryService.type }}
  selector:
    app: woodpecker-discovery
  {{- include "spring-cloud-helm.selectorLabels" . | nindent 4 }}
  ports:
	{{- .Values.discoveryService.ports | toYaml | nindent 2 -}}
