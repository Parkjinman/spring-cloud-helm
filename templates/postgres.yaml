apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
  {{- include "spring-cloud-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.postgres.type }}
  selector:
    app: woodpecker-postgres
  {{- include "spring-cloud-helm.selectorLabels" . | nindent 4 }}
  ports:
	{{- .Values.postgres.ports | toYaml | nindent 2 -}}
